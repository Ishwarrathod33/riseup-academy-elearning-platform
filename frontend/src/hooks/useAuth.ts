/** Re-export for convenient imports: `import { useAuth } from "@/hooks/useAuth"` */
export { useAuth } from "@/features/auth/AuthProvider";
export type { AuthContextValue } from "@/features/auth/AuthProvider";
