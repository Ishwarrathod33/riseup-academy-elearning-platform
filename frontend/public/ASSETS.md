# Static assets

- **`logo.png`** — Brand logo used by `BrandMark` in `src/components/layout/BrandMark.tsx` (navbar + footer). Replace this file to update branding everywhere.

Tailwind / `_next` 404s: run `npm run dev` from the **`frontend`** folder, delete `.next` if needed, hard-refresh the browser.
